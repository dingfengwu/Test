# -------------------------------SQL语句文件说明------------------------------------------
# "#"为整行注解符
#
# 空一行表示这条语句结束
#
# 如果SQL语句只有一行时：
# 每行的第一个"="号为key与sql语句的分割符
#  
# 如果SQL语句存在多行时：
# 第一行的第一个"="号为key与sql语句的分割符
#
# 例如：
# Key1=SELECT * FROM TABLE
#
# KEY2=SELECT *
#		FROM TABLE
#

#查询客户
key1=SELECT * FROM Customer

